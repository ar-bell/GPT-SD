
function flipCard() {
    const flashcard = document.getElementById('flashcard');
    if (flashcard) {
        flashcard.classList.toggle('flipped');
    }
}

let currentCard = 1;
let totalCards = 1;

function updateProgress() {
    console.log('Progress update for now');
    }